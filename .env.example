# Supabase — publiczne (anon key; OK w repo)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Sentry — DSN publiczny; AUTH_TOKEN to sekret (nie commituj realnej wartości)
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# SERVICE ROLE — NIGDY tutaj; tylko .env.local + GH Actions secret
# SUPABASE_SERVICE_ROLE_KEY=  ← usunięte celowo; patrz README
